/* 5) Реалізувати логіку підрахунку ціни товару по його варіаціях(шаблон наведений тут, при кліку на колір змінювати ціну товару).
Придумати ще 2 варіації, відяких буде залежати ціна товару.
*/ 



























///// Function for creating elements
// const createTag = (element, elementName) =>
//   elementName = document.createElement(element);
 
///// Function for adding classes
// const addClass = (elementName, className) => 
  // elementName.classList.add(className);

// const divTag = createTag('div', 'container');
// addClass(divTag, 'container');



// document.body.append(divTag);

// createTag('div', 'container');
// addClass(createTag('div', 'container'), 'container');